# dependencies
node_modules/
.agents
skills-lock.json
# build output
dist/
build/

# vite caches
.vite/

# typescript
*.tsbuildinfo

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (opt-in if a future deploy ever needs one)
.env
.env.*
!.env.example

# bulletin / dotNS artifacts
*.car
out.car
out.bulletin.car

# Local mnemonic capture from deploy.sh — never commit signer material
.deploy-mnemonic
.deploy-mnemonic.*

# Vendored product-sdk tarballs ARE committed on purpose — they pin the
# host SDK surface w3spay-admin was built against. Do not add tools/ here.

# Sentry Config File
.env.sentry-build-plugin
