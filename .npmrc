# `@parity/product-sdk-host@0.3.0` declares `peerOptional`
# `@novasamatech/host-api: ">=0.6.0"`. We pin to `0.7.8-2`, a prerelease,
# and npm v10's strict resolver excludes prereleases from non-prerelease
# ranges. Same workaround the t3rminal monorepo uses.
legacy-peer-deps=true
